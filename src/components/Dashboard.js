import React, { useState } from "react";

export const Dashboard = (props) => {
    return (
        <>
            <button>Strike</button>
            <button>Ball</button>
            <button>Foul</button>
            <button>Hit</button>
        </>
    )
}